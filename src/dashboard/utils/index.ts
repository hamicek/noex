/**
 * Dashboard utilities module.
 *
 * Exports formatting and helper functions used by dashboard widgets.
 */

export {
  formatNumber,
  formatUptime,
  formatTime,
  truncate,
  calculatePercent,
  formatReason,
  TreeChars,
} from './formatters.js';
