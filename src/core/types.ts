/**
 * Core type definitions for noex GenServer/Supervisor pattern.
 * These types will be implemented in subsequent steps.
 */

// Placeholder export to ensure module is valid
export type Placeholder = never;
